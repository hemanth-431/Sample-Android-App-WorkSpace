<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.farmerfriendspro">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WheatActivity"></activity>
        <activity android:name=".TomatoActivity" />
        <activity android:name=".tobaccoActivity" />
        <activity android:name=".ScaneActivity" />
        <activity android:name=".RiceActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".JuteActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CottonActivity" />
        <activity android:name=".CoffeeActivity" />
        <activity android:name=".ChilliActivity" />
        <activity android:name=".CarrotActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>